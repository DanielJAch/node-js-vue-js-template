import home from './home';
import navigation from './navigation';

const state = {
  routes: [
    home,
    navigation
  ]
};

const actions = {
};

const mutations = {
};

export default {
  state,
  actions,
  mutations
};